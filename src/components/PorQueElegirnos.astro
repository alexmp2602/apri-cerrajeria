---
import IconClock24 from "./icons/IconClock24.astro";
import IconMapPin from "./icons/IconMapPin.astro";
import IconShieldCheck from "./icons/IconShieldCheck.astro";
import IconTools from "./icons/IconTools.astro";
---

<section id="porque-elegirnos" class="bg-[#1a1a1a] text-white p-6 pb-12 lg:py-20">
  <div class="max-w-6xl mx-auto text-center space-y-12">
    <!-- Título -->
    <div>
      <h2 class="text-2xl sm:text-4xl font-bold mb-4">¿Por qué elegirnos?</h2>
      <p class="text-white/70 text-lg max-w-2xl mx-auto">
        Más de una razón para confiar en APRI cuando se trata de tu seguridad.
      </p>
    </div>

    <!-- Grilla de beneficios -->
    <div class="grid gap-10 sm:grid-cols-2 lg:grid-cols-4">
      <!-- 1. Atención 24/7 -->
      <div class="flex flex-col items-center text-center space-y-4">
        <IconClock24 className="w-9 h-9 sm:w-12 sm:h-12 text-yellow-400" />
        <h3 class="font-semibold text-lg">Atención 24/7</h3>
        <p class="text-white/60 text-sm">
          Estamos disponibles todos los días, a toda hora.
        </p>
      </div>

      <!-- 2. Confianza garantizada -->
      <div class="flex flex-col items-center text-center space-y-4">
        <IconShieldCheck className="w-9 h-9 sm:w-12 sm:h-12 text-yellow-400" />
        <h3 class="font-semibold text-lg">Confianza garantizada</h3>
        <p class="text-white/60 text-sm">
          Años de experiencia y cientos de clientes satisfechos.
        </p>
      </div>

      <!-- 3. Técnicos capacitados -->
      <div class="flex flex-col items-center text-center space-y-4">
        <IconTools className="w-9 h-9 sm:w-12 sm:h-12 text-yellow-400" />
        <h3 class="font-semibold text-lg">Técnicos capacitados</h3>
        <p class="text-white/60 text-sm">
          Usamos herramientas de calidad y formación continua.
        </p>
      </div>

      <!-- 4. Cobertura en Mendoza -->
      <div class="flex flex-col items-center text-center space-y-4">
        <IconMapPin className="w-9 h-9 sm:w-12 sm:h-12 text-yellow-400" />
        <h3 class="font-semibold text-lg">Cobertura en Mendoza</h3>
        <p class="text-white/60 text-sm">
          Llegamos rápido a cualquier punto de la ciudad.
        </p>
      </div>
    </div>
  </div>
</section>
